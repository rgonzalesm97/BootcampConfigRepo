server:
  port: 8086
eureka:
  client:
    fetch-registry: true
    register-eith-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost
